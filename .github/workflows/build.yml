name: Windows Exe Derleme

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Kodu Cek
        uses: actions/checkout@v4

      - name: Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Bagimliliklari Yukle
        run: |
          python -m pip install --upgrade pip
          pip install opencv-python mediapipe pillow numpy pyinstaller

      - name: Exe Olustur
        run: |
          pyinstaller --noconsole --onefile --collect-all mediapipe --hidden-import=mediapipe --hidden-import=mediapipe.python --hidden-import=mediapipe.python.solutions --hidden-import=mediapipe.python.solutions.hands --hidden-import=mediapipe.python.solutions.drawing_utils --add-data "selamlama.gif;." main.py

      - name: Exe Dosyasini Yukle
        uses: actions/upload-artifact@v4
        with:
          name: SelamlamaProgrami-Windows
          path: dist/*.exe
